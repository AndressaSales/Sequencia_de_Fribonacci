let anterior= 0;
let atual = 1;
let = proximo = 1;

const numero = prompt('Digite um número:');
document.write('Sequencia de Fibonacci');

for (let i = 0; i < numero; i++){
    document.write("<br>" +proximo);
    anterior = atual + proximo;
    atual = proximo
    proximo = anterior

}
